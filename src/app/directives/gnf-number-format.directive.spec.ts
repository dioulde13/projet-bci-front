import { GnfNumberFormatDirective } from './gnf-number-format.directive';

describe('GnfNumberFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new GnfNumberFormatDirective();
    expect(directive).toBeTruthy();
  });
});
